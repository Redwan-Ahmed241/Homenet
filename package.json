{
  "name": "homenet-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "SaaS Property Marketplace Bangladesh",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "api": "npm run start:dev -w apps/api"
  },
  "dependencies": {
    "@vercel/speed-insights": "^2.0.0"
  }
}
