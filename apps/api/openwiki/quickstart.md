# OpenWiki Quickstart for Homenet API

Welcome to the OpenWiki documentation for the **Homenet** API (NestJS).

## Overview
The repository implements a **NestJS** server that provides a REST API for a property‑listing platform.  Key technical pillars are:

- **NestJS** modular architecture (Auth, User, Role, Area, Property, etc.)
- **Prisma** ORM with a PostgreSQL database (see `prisma/schema.prisma`)
- **RBAC** with permissions, decorators and guards (`src/modules/role/*`)
- **JWT** based authentication and local‑strategy login (`src/modules/auth/*`)
- Centralised **error handling** and **API response format** (`src/common/*`)
- **Winston** logger module (`src/common/logger/*`)
- **Cache Service** centralized caching (`src/common/cache/*`)
- **Rate limiting** via `@nestjs/throttler`
- Comprehensive **unit and e2e tests** (`test/`)

## Getting Started
1. **Clone the repo** and install dependencies:
   ```bash
   npm install
   ```
2. **Configure env** – copy `.env.example` to `.env` and fill required values (`DATABASE_URL`, `JWT_SECRET`, etc.).
3. **Database** – run Prisma migration (see `docs/db-dev-guide.md`).
4. **Run locally**:
   ```bash
   npm run start:dev   # watch mode
   ```
   API docs are available at `http://localhost:3000/api/docs`.
5. **Run tests**:
   ```bash
   npm run test        # unit tests
   npm run test:e2e   # e2e tests
   ```

## Where to Go Next
| Section | What you’ll learn | Link |
|---|---|---|
| **Architecture** | High‑level view of modules, Prisma integration, global guards, logger, throttling | [architecture.md](architecture.md) |
| **Authentication** | JWT flow, local strategy, DTOs, guards, Swagger docs | [authentication.md](authentication.md) |
| **RBAC (Roles & Permissions)** | Role & permission models, `@Permissions` decorator, guard implementation | [rbac.md](rbac.md) |
| **Error Handling** | Unified error codes, exception filter, response interceptor | [error-handling.md](error-handling.md) |
| **Logging** | Winston logger module, configuration, usage patterns | [logging.md](logging.md) |
| **Testing** | Unit/E2E test organization, how to add new tests | [testing.md](testing.md) |

## Helpful Docs in `/docs`
- **Build & Run Guide** – `docs/build-guide.md`
- **Database Development Guide** – `docs/db-dev-guide.md`
- **Error Codes Reference** – `docs/error-codes.md`
- **RBAC Guide** – `docs/rbac-guide.md`

## Contributing
When extending the API, follow the patterns described in the relevant sections above and run the full test suite (`npm run test && npm run test:e2e`).

---
*This quickstart is generated by OpenWiki. For deeper insights, follow the links above.*
